    .origin 0x0010
    mov a, 0x0
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    inc a, a
    .dw 0xffff, 0xffff, 0xffff
